name: selfcheckoutapp
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  # Google Auth
  #google_sign_in: ^5.0.2
  # Firebase
  firebase_core: ^1.1.0
  firebase_auth: ^1.1.3
  cloud_firestore: ^2.0.0
  # Google Fonts
  google_fonts: ^2.0.0
  #device_preview: ^0.5.5
  # Barcode Scanner
  flutter_barcode_scanner: ^2.0.0
  # In app Saving
  shared_preferences: ^2.0.5
  # Loading
  flutter_spinkit: ^5.0.0
  # Payment
  flutter_credit_card: ^2.0.0
  stripe_payment: ^1.1.0
  http:
  progress_dialog: ^1.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
     - assets/image2.png
     - assets/image2-dark.png
